#include "main.h"
#include <string.h>

/**
 * _strlen - Function that returns the length of a string.
 * @s: Input string
 * Return: Nothing
 */
int _strlen(char *s)
{
	int b;

	b = strlen(s);
	return (b);
}
