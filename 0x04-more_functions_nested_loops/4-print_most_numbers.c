#include "main.h"

/**
*print_most_numbers - prints the numbers,from 0 to 9 and ommit 2 and 4
*_putchar: function to be used
*
*/

void print_most_numbers(void)
{
	char b;

	for (b >= 48; b <= 58; b++)
	{
	if (b != 50 && b != 52)
	}
	putchar(b);
	}
	}
	putchar ('\n');
}
